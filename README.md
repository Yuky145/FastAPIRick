# FastAPIRick
Prueba de Fast API
Nota: Instalar el entorno virtual se puede usar este comando: python -m venv .venv
- Instalar el fastapi utilizando este comando: pip install "fastapi[standard]"
- Instalar uvicorn utilizando este comando: pip install uvicorn
- Instalar sqlmodel utilizando este comando: pip install sqlmodel
